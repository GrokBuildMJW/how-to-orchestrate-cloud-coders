## Context

Parent: <EPIC>.
You already decided: <one closed sentence>.
Do not reopen a closed fork.
No new skill unless this ticket requires one.

What exists today. Name who writes, who reads, who calls, and how
long the change lives. No "A vs B". Name required fields of types
that already exist.

## Definition of done

Every rule the checker must enforce is a checkbox here, not only
in Context.

- [ ] <writer behavior>
- [ ] <reader / caller behavior>
- [ ] Proof that the old path fails: restore the old line and that
      test goes red
- [ ] Docs + changelog in the same change
- [ ] No new skill unless this ticket requires one

## Parent

<EPIC>
